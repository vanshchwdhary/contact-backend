services:
  - type: web
    name: contact-backend
    env: node
    buildCommand: "npm install"
    startCommand: "npm start"
    envVars:
      - key: MAIL_USER
        sync: false
      - key: MAIL_PASS
        sync: false