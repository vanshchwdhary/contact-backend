services:
  - type: web
    name: contact-backend
    env: node
    region: singapore
    buildCommand: "npm install"
    startCommand: "npm start"
    plan: free
    envVars:
      - key: MAIL_USER
        fromDatabase: false
      - key: MAIL_PASS
        fromDatabase: false